.DS_Store
.venv/

# Full-length master. Not deployed and not needed at runtime — it is only the
# input to prep.py. Original: ~/Downloads/GNC Flyover.mp4.txt (a real MP4
# despite the extension). Keep a copy somewhere durable.
assets/flyover-source.mp4

# Same resolution as hero.h264.mp4: the source is 1920 wide, so the ladder's
# "scale to 1920" rung is a no-op and we never upscale. Nothing loads it — the
# sub-1280px tier uses hero-1280.*. prep.py still regenerates it.
assets/hero-1080.mp4

# Self-contained share build (~7 MB); regenerate with scripts/build-preview.py
build/
